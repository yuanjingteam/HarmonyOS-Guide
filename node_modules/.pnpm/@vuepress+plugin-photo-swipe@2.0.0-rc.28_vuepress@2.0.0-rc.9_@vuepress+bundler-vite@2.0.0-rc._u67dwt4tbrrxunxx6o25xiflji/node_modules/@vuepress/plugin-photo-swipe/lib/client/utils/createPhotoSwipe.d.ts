import type { PhotoSwipeOptions } from '../helpers/index.js';
export interface PhotoSwipeState {
    open: (index: number) => void;
    close: () => void;
    destroy: () => void;
}
export declare const createPhotoSwipe: (images: string[], photoSwipeOptions: PhotoSwipeOptions, scrollToClose?: boolean) => Promise<PhotoSwipeState>;
