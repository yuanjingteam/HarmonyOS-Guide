import type { PhotoSwipePluginLocaleData } from '../../shared/index.js';
import 'photoswipe/dist/photoswipe.css';
import '../styles/photo-swipe.css';
export interface UsePhotoSwipeOptions {
    selector: string | string[];
    locales: Record<string, Record<`${keyof PhotoSwipePluginLocaleData}Title`, string>>;
    /** @default 500 */
    delay?: number;
    /** @default true */
    scrollToClose?: boolean;
}
export declare const usePhotoSwipe: ({ selector, locales, delay, scrollToClose, }: UsePhotoSwipeOptions) => void;
