import type { App } from 'vuepress/core';
export declare const prepareConfigFile: (app: App, id: string) => Promise<string>;
export declare const prepareInjectSass: (app: App, id: string) => Promise<string>;
export interface PrepareConfigOptions {
    id: string;
    defaultConfig: string;
    defaultPalette: string;
    generator: string;
    userConfig: string;
    userPalette: string;
}
export declare const prepareConfigSass: (app: App, { id, defaultConfig, defaultPalette, generator, userConfig, userPalette, }: PrepareConfigOptions) => Promise<string>;
export interface PreparePaletteOptions {
    id: string;
    defaultPalette: string;
    generator: string;
    userPalette: string;
}
export declare const preparePaletteSass: (app: App, { id, defaultPalette, generator, userPalette }: PreparePaletteOptions) => Promise<string>;
export interface PrepareStyleOptions {
    id: string;
    userStyle: string | null;
}
export declare const prepareStyleSass: (app: App, { id, userStyle }: PrepareStyleOptions) => Promise<string | null>;
